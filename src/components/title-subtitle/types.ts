export interface TitleSubtitleProps {
	title: string;
	subTitle: string;
	isLoading?: boolean;
	marginT?: string;
	marginB?: string;
}

export interface TitleSubtitleContainerProps {
	marginT: string;
	marginB: string;
}
